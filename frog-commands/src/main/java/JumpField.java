public class JumpField {
    final int MIN_POSITION, MAX_POSITION;

    public JumpField(int min_position, int max_position) {
        MIN_POSITION = min_position;
        MAX_POSITION = max_position;
    }
}
